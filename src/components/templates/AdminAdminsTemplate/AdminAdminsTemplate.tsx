import React from 'react';
import { Typography } from '@/components/ui/typography';

export default function AdminAdminsTemplate() {
  return (
    <div className="p-4">
      <Typography variant="h3" className="mb-2">Admins Template</Typography>
      <Typography className="text-muted-foreground">This is the template for the Admins admin page.</Typography>
    </div>
  );
}
